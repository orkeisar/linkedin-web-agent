// Shared encode/decode helpers for the admin.html -> index.html "c" query param handoff.
// Used by both app.js (decode on first load) and admin.js (encode on link generation).
// Implemented in Phase 2/3.
