// Bootstrap + view routing for index.html: first-load detection (decode
// the `c` param via linkConfig.js, seed pillars, strip the URL), then
// route between onboarding, board, drafting, and settings views.
// Implemented starting Phase 1 (key gate + nav shell).

document.addEventListener("DOMContentLoaded", () => {
  console.log("LinkedIn Story Pipeline — app.js loaded (Phase 1 stub)");
});
